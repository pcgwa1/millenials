import colors, { gradients } from './colors';
import fonts from './fonts';
import breakpoints from './breakpoints';

const theme = {
  colors, gradients, fonts, breakpoints,
};

export default theme;
